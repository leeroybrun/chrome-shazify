/*chrome.cookies.onChanged.addListener(function(info) {
	if(info.cookie.domain.indexOf('shazam.com') != -1) {
		var popupUrl = chrome.extension.getURL('src/popup/popup.html');
		focusOrCreateTab(popupUrl);
	}
});*/